{
  "name": "url2pdf",
  "version": "0.5.0",
  "description": "converts urls to pdf files",
  "main": "url2pdf.js",
  "bin": "url2pdf.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dist-win": "pkg -t latest-win-x64 -o ./dist/url2pdf-win64 ./package.json"
  },
  "author": "@drodsou",
  "license": "ISC",
  "dependencies": {
    "puppeteer-core": "1.17.0"
  },
  "devDependencies": {
    "pkg": "^4.4.0"
  }
}
